# QGIS 核心缩放实验备份

原则上只修改自己的插件。本次核心修改是实验性例外。

- `before/`：撤回本轮缩放实验后的 7 个源文件。
- `after/`：对应实验版快照。
- `manifest.json`：重建来源与 SHA-256 校验值。

这是根据已记录改动事后重建并核验的源码备份，不是事前快照或 DLL 备份。
核心文件已反向重建并正向重放验证，dock 保留之前未提交的功能。

在插件目录执行 `python patches/restore_close_zoom_backup.py` 仅校验。
用户决定回退后，关闭 QGIS，在有核心目录写权限的环境中加 `--apply` 执行，
再重新编译并部署 `qgis_3d` 和 `plugin_parammodeler`。

当前状态：已按用户要求恢复全部 7 个源文件，未替换或重新构建 DLL。
实验专用头文件和数学测试已移入 `experimental-only/`，仅作档案。

必须同时恢复核心和配套插件。若文件有后续修改，脚本会拒绝覆盖，
届时依据 before/after 差异定向撤回实验，不覆盖其他功能。
恢复并非跨文件原子操作；如遇磁盘或权限错误，排查后重新校验，
已恢复的文件会被跳过。执行期间不要并发编辑这些文件。
